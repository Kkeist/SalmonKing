# 三文鱼之神 · Salmon King

我要吃很多很多的三文鱼……

//测试限定（测试完毕删除本段，现在还在测试，禁止删除）
- 每次修改游戏（逻辑 / 布局 / 资源）时，请同步更新：
  - `netlify.toml` 中 `[context.production.environment].APP_VERSION`
  - `index.html` 顶部脚本中的 `APP_VERSION` 常量
  这样可以通过 Netlify 部署和标题页右下角的版本号快速确认是否为最新版本。
